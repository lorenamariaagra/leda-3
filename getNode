    private Node getNode(int index) {
        if(index < 0 || index >= size)
            throw new RunTimeException();
        Node atual = this.head;
        for(int i = 0; i < index; i++){
            atual = atual.next;
        }
        return atual;
    }

\\APONTAR PARA O PROPRIO NODE
Node aux = this.head;
for(int i =0; i < index; i++){
    aux = aux.next;
    }
aux.prev.next 
aux.next.prev
